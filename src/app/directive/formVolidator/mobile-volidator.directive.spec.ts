import { MobileVolidatorDirective } from './mobile-volidator.directive';

describe('MobileVolidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileVolidatorDirective();
    expect(directive).toBeTruthy();
  });
});
